package validators

import "thesis-management-app/types"

func ValidateEmployee(t types.UniversityEmployee) (types.UniversityEmployeeErrors, bool) {
	return types.UniversityEmployeeErrors{}, true //if validation is needed, idk if it is tbh
}
