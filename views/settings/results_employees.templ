package settings

import "thesis-management-app/types"

templ ResultsUsers(empl_data []types.UniversityEmployee) {
    <div id="results">
    for _, user := range empl_data {
            @Entry(user)
        <br />
    }
    </div>
}
